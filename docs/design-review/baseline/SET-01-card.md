# 基准卡：SET-01 风险与投资约束

> 补卡（审计回填 2026-09-09）：V4 已实现页的追溯基准卡。硬限制即时生效、
> 修改写审计（risk_saved 提示）。比例字段以小数存储（如 0.30）。

## 1. 参考稿

- 移动：`stitch/mobile/SET-01-mobile.html`（另有 -cn 中文变体，文案取中文版口径）
- 桌面：`stitch/SET-01-desktop.html`（响应式：表单卡展宽）

## 2. 布局结构

```
1. PageHeader（title「风险与约束」+ subtitle「硬限制即时生效 · 修改写审计」）
2. 参数表单卡（字段：单标的仓位上限 / 行业集中度上限 / 最低现金比例 /
   前 N 持仓 / 前 N 集中度上限；比例项标注「小数，如 0.30」）
3. 保存条（保存按钮 + 成功提示 risk-saved）
```

- 栅格/分栏：单列表单
- 滚动行为：整页滚动
- 桌面差异：无（响应式同构）

## 3. 关键尺寸

| 位置 | 稿实测 | 落点 token |
| --- | --- | --- |
| 字段标签 | — | `text-label text-gray-500` |
| 输入框 | — | 全局 input 基类 + `num` |
| 卡片 | — | `p-4` + `rounded-lg` |
| 成功提示 | — | `text-caption text-success` |

## 4. 组件清单

| 组件 | 需覆盖 variant |
| --- | --- |
| 参数字段 | 比例（带小数提示）/ 整数 |
| 保存 | idle / submitting / 成功提示 |
| 错误 | 服务端校验失败行内提示 |

## 5. 稿内违例覆盖注记（disregarded）

| 稿位置 | 违例内容 | 覆盖依据 |
| --- | --- | --- |
| 稿百分比输入形态 | 与后端小数存储不符 | 以 settings API 契约为准（小数） |
| 稿英文文案（通用版） | 英文占位 | 中文文案规范（-cn 变体同口径） |

## 6. 走查项打勾区

- [ ] 三截图并排贴 PR
- [x] 默认 / 加载 / 空 / 异常四态
- [x] 门禁全绿；深色 dark: 核验（V5 扫描清零）

---

## 勘误增补：视觉语言字段（2026-09-11 W3-C）

> V1 建卡时未收录视觉语言三要素，致 V3/V4 实现'结构对齐但观感不像'（见 11_LAUNCH_QUALITY_PLAN §0/W3-C）。W3 起以下字段为建卡必填，本卡按已上线的 W3 视觉基线（material-symbols-rounded 图标系 / 卡片 rounded-2xl / 彩色 icon 容器 / 系统字体栈）回填勘误。

| 字段 | 本页基线 |
| --- | --- |
| 图标体系 | material-symbols-rounded（构建期内联）；导航/卡头图标与原型稿一一对位 |
| 圆角档位 | 卡片 rounded-2xl（16px）；按钮/输入 rounded-md/lg；禁用其他档位 |
| 色彩容器 | 摘要/入口卡 icon 容器：语义色 10% 底 + 同色 icon（primary/market/risk 族） |
| 字体 | 系统栈（SF/Segoe + 苹方/雅黑）+ antialias（Q2-A 决策，弃 Inter） |

